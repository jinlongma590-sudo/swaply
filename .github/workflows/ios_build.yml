name: iOS App Store Build

on:
  workflow_dispatch:

jobs:
  build_ios:
    name: Build IPA
    runs-on: macos-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: 'stable'
          cache: true

      - name: Install Certificates & Profile
        env:
          BUILD_CERTIFICATE_BASE64: ${{ secrets.BUILD_CERTIFICATE_BASE64 }}          # p12 (含私钥)
          BUILD_CERTIFICATE_CER_BASE64: ${{ secrets.BUILD_CERTIFICATE_CER_BASE64 }}  # 可选 .cer（中级/发行证书链）
          P12_PASSWORD: ${{ secrets.P12_PASSWORD }}                                  # p12 密码
          BUILD_PROVISION_PROFILE_BASE64: ${{ secrets.BUILD_PROVISION_PROFILE_BASE64 }} # .mobileprovision
          KEYCHAIN_PASSWORD: "temporary_password"
        shell: bash
        run: |
          set -euo pipefail

          CERTIFICATE_P12_PATH="$RUNNER_TEMP/build_certificate.p12"
          CERTIFICATE_CER_PATH="$RUNNER_TEMP/build_certificate.cer"
          PP_PATH="$RUNNER_TEMP/build_pp.mobileprovision"
          PROFILE_PLIST="$RUNNER_TEMP/profile.plist"
          KEYCHAIN_PATH="$RUNNER_TEMP/app-signing.keychain-db"

          # 0) 还原文件（macOS 用 -D）
          if [ -n "${BUILD_CERTIFICATE_BASE64:-}" ]; then
            printf "%s" "$BUILD_CERTIFICATE_BASE64" | base64 -D > "$CERTIFICATE_P12_PATH"
          fi
          if [ -n "${BUILD_CERTIFICATE_CER_BASE64:-}" ]; then
            printf "%s" "$BUILD_CERTIFICATE_CER_BASE64" | base64 -D > "$CERTIFICATE_CER_PATH"
          fi
          printf "%s" "$BUILD_PROVISION_PROFILE_BASE64" | base64 -D > "$PP_PATH"

          # 1) 创建并设置默认钥匙串 + 搜索列表
          security create-keychain -p "$KEYCHAIN_PASSWORD" "$KEYCHAIN_PATH"
          security set-keychain-settings -lut 21600 "$KEYCHAIN_PATH"
          security unlock-keychain -p "$KEYCHAIN_PASSWORD" "$KEYCHAIN_PATH"
          security default-keychain -s "$KEYCHAIN_PATH"
          security list-keychains -d user -s "$KEYCHAIN_PATH"

          # 2) 导入 Apple WWDR（中间证书，可选）
          curl -fsSL -o "$RUNNER_TEMP/AppleWWDRCAG3.cer" https://www.apple.com/certificateauthority/AppleWWDRCAG3.cer || true
          if [ -s "$RUNNER_TEMP/AppleWWDRCAG3.cer" ]; then
            security import "$RUNNER_TEMP/AppleWWDRCAG3.cer" -k "$KEYCHAIN_PATH" -t cert -f der -T /usr/bin/codesign -T /usr/bin/security || true
          fi

          # 3) 导入你提供的 .cer（若有）
          if [ -s "$CERTIFICATE_CER_PATH" ]; then
            security import "$CERTIFICATE_CER_PATH" -k "$KEYCHAIN_PATH" -t cert -f der -T /usr/bin/codesign -T /usr/bin/security || true
          fi

          # 4) 导入 .p12（必须包含私钥）
          if [ -s "$CERTIFICATE_P12_PATH" ]; then
            if [ -z "${P12_PASSWORD:-}" ]; then
              echo "[info] P12_PASSWORD 为空：尝试不带密码导入"
              security import "$CERTIFICATE_P12_PATH" \
                -k "$KEYCHAIN_PATH" -f pkcs12 -A \
                -T /usr/bin/codesign -T /usr/bin/security -T /usr/bin/xcodebuild
            else
              security import "$CERTIFICATE_P12_PATH" \
                -k "$KEYCHAIN_PATH" -f pkcs12 -P "$P12_PASSWORD" -A \
                -T /usr/bin/codesign -T /usr/bin/security -T /usr/bin/xcodebuild
            fi
          fi

          # 5) 允许 codesign/xcodebuild 访问私钥
          security set-key-partition-list -S apple-tool:,apple: -s -k "$KEYCHAIN_PASSWORD" "$KEYCHAIN_PATH"

          # 6) 验证 identity（应 >= 1）
          security find-identity -v -p codesigning "$KEYCHAIN_PATH"

          # 7) 安装描述文件
          mkdir -p "$HOME/Library/MobileDevice/Provisioning Profiles"
          cp "$PP_PATH" "$HOME/Library/MobileDevice/Provisioning Profiles/"

          # 8) 解析描述文件
          security cms -D -i "$PP_PATH" > "$PROFILE_PLIST"
          UUID=$(/usr/libexec/PlistBuddy -c 'Print UUID' "$PROFILE_PLIST")
          NAME=$(/usr/libexec/PlistBuddy -c 'Print Name' "$PROFILE_PLIST")
          TEAM_ID=$(/usr/libexec/PlistBuddy -c 'Print TeamIdentifier:0' "$PROFILE_PLIST")
          APP_ID=$(/usr/libexec/PlistBuddy -c 'Print Entitlements:application-identifier' "$PROFILE_PLIST")
          BUNDLE_ID="${APP_ID#${TEAM_ID}.}"

          echo "Profile UUID: $UUID"
          echo "Profile Name: $NAME"
          echo "Team ID: $TEAM_ID"
          echo "Bundle ID (from profile): $BUNDLE_ID"

          {
            echo "PP_UUID=$UUID"
            echo "PP_NAME=$NAME"
            echo "PP_TEAM=$TEAM_ID"
            echo "PP_BUNDLE_ID=$BUNDLE_ID"
          } >> $GITHUB_ENV

      - name: Fix Xcode Signing (Manual + Specifier by Name)
        shell: bash
        run: |
          set -euo pipefail
          PBX_PATH="ios/Runner.xcodeproj/project.pbxproj"

          echo "Using TEAM: $PP_TEAM / PROFILE: $PP_NAME (UUID: $PP_UUID) / BUNDLE: $PP_BUNDLE_ID"

          # 1) 强制 Manual
          sed -i '' 's/CODE_SIGN_STYLE = Automatic;/CODE_SIGN_STYLE = Manual;/g' "$PBX_PATH"

          # 2) 统一签名身份为 Apple Distribution（Xcode 13+）
          sed -i '' 's/CODE_SIGN_IDENTITY = "[^"]*";/CODE_SIGN_IDENTITY = "Apple Distribution";/g' "$PBX_PATH"
          sed -i '' 's/CODE_SIGN_IDENTITY\[sdk=iphoneos\*\] = "[^"]*";/CODE_SIGN_IDENTITY[sdk=iphoneos*] = "Apple Distribution";/g' "$PBX_PATH"

          # 3) 设置团队
          sed -i '' "s/DEVELOPMENT_TEAM = [A-Z0-9]*/DEVELOPMENT_TEAM = $PP_TEAM/g" "$PBX_PATH"

          # 4) 设置 Specifier（用 Name，而非 UUID；兼容已有/未有两种情况）
          if grep -q 'PROVISIONING_PROFILE_SPECIFIER = ' "$PBX_PATH"; then
            sed -i '' "s/PROVISIONING_PROFILE_SPECIFIER = \".*\";/PROVISIONING_PROFILE_SPECIFIER = \"$PP_NAME\";/g" "$PBX_PATH"
          else
            sed -i '' "/PRODUCT_BUNDLE_IDENTIFIER = ${PP_BUNDLE_ID//./\\.}/a\\
            PROVISIONING_PROFILE_SPECIFIER = \"$PP_NAME\";" "$PBX_PATH"
          fi

          # （可选）若还遗留 PROVISIONING_PROFILE（老字段，UUID），同步一下
          if grep -q 'PROVISIONING_PROFILE = ' "$PBX_PATH"; then
            sed -i '' "s/PROVISIONING_PROFILE = \".*\";/PROVISIONING_PROFILE = \"$PP_UUID\";/g" "$PBX_PATH"
          fi

      - name: Install Dependencies
        run: |
          flutter pub get
          cd ios
          pod install --repo-update

      - name: Build IPA
        run: |
          # ✅ 修复点：去掉不被 Flutter 支持的 -xcode-build-settings 传参
          flutter build ipa --release --export-method=app-store --verbose

      - name: Upload IPA
        uses: actions/upload-artifact@v4
        with:
          name: swaply-release-ipa
          path: build/ios/ipa/*.ipa
          retention-days: 1
